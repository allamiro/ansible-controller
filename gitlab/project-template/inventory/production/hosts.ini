# Replace this documentation address. The controller environment map must
# select this file's DIRECTORY (inventory/production), so that encrypted
# group_vars beside it are staged with the inventory in mesh mode.
[deployment]
app01 ansible_host=192.0.2.10 ansible_user=svc_ansible
